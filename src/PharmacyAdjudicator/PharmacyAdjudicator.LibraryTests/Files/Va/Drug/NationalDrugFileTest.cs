using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.IO;

using PharmacyAdjudicator.Library;

namespace PharmacyAdjudicator.LibraryTests.Files.Va.Drug
{
    [TestClass]
    public class NationalDrugFileTest
    {
        //[TestMethod]
        //public void CanReadFile()
        //{
        //    //FileStream fs = new FileStream("/TestFiles.ndf_csv.txt", StreamReader);
        //    FileStream fs = System.IO.File.OpenRead("../../TestFiles/ndf.txt");
        //    StreamReader reader = new StreamReader(fs);
        //    char delimiter = '|';

        //    Library.Files.Va.Drug.NationalDrugFile drugFile = new Library.Files.Va.Drug.NationalDrugFile(reader, delimiter);
        //    Assert.IsTrue(drugFile.Count > 100000, "Drug file too small");
        //}
    }
}

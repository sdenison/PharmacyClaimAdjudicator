<UserControl x:Class="PharmacyAdjudicator.ModernUI.Welcome.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
       <ScrollViewer Grid.Row="0" VerticalAlignment="Top">
            <StackPanel MinWidth="200">
                <TextBlock Text="Hello." Style="{StaticResource Heading2}" />
                <mui:BBCodeBlock Grid.Row="0" Margin="0,0,0,5" BBCode="{Binding WelcomeMessage}" />
            </StackPanel>
        </ScrollViewer>
        <!--<DockPanel Grid.Row="1" VerticalAlignment="Bottom">
            <ProgressBar Minimum="1" Maximum="1" Height="16" IsIndeterminate="True" Margin="0,0,0,16" VerticalAlignment="Bottom" />
        </DockPanel>-->
    </Grid>
</UserControl>
